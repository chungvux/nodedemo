const minute = 1000 * 60;
const hour = minute * 60;
const day = hour * 24;
const month = day*30;
const year = day * 365;

module.exports = {
    MINUTE :minute,
    HOUR :hour,
    DAY:day,
    MONTH:month,
    YEAR:year
}